<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jmp.rickandmortycharacters">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".RickAndMortyCharactersApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_rym"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_rym_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RickAndMortyCharacters">

        <activity
            android:name=".main.view.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RickAndMortyCharacters" />

        <activity
            android:name=".splash.view.SplashActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
